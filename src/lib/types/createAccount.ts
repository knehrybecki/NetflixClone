export const enum StepsNumber {
	STEP_ONE = 1,
	STEP_TWO = 2,
	STEP_THREE = 3,
}

export enum createAccoutTypes {
	ACTIVATE_STEP = 'ACTIVATE_STEP',
	ANIMATION = 'ANIMATION',
	STEPS = 'STEPS',
	SELECTED_PLAN = 'SELECTED_PLAN',
}

export const AnimationRegister = {
	fade: 'fade',
	flyLeft: 'fly left',
	flyRight: 'fly right',
	druation: 500,
}
